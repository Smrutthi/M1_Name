name: Build CI - Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: Windows

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: make 3_Implementation
